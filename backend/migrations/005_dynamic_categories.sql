-- No schema changes needed since allocation_preferences already serves as the categories table.
-- We just need to ensure the API supports full CRUD on allocation_preferences.
-- This migration is a no-op placeholder for versioning.
SELECT 1;
